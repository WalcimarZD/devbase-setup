# Credentials Vault

> Sensitive credentials - NEVER sync with cloud

## Usage

Store here:
- API keys
- Access tokens
- Certificates
- .env files

## Security

This directory is in .gitignore and should NEVER be synchronized.
Use tools like:
- 1Password CLI
- Azure Key Vault
- HashiCorp Vault
- git-crypt
