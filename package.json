{
  "name": "devbase-setup",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"No tests defined\" && exit 0",
    "lint": "echo \"No linter configured\" && exit 0"
  },
  "devDependencies": {}
}
