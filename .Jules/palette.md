## 2024-05-23 - Interactive CLI Arguments
**Learning:** Making CLI arguments optional and prompting for them (instead of failing) significantly improves the onboarding experience and reduces friction for new users who might forget the exact syntax.
**Action:** Identify other "required" arguments in CLI commands that could be made optional with interactive prompts (e.g., project names, search queries).
